nelmio_cors:
    defaults:
        allow_origin: ["*"]
        allow_methods: ["POST", "OPTIONS"]
        allow_headers: ["Content-Type", "Authorization"]
        max_age: 3600
    paths:
        "^/login":
            allow_origin: ["*"]
            allow_methods: ["POST", "OPTIONS"]
            allow_headers: ["Content-Type", "Authorization"]
